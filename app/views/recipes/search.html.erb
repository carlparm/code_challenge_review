<h1>Recipes with <%= params[:q] %> ingredients</h1>

<ul>
    <% @search_result.each do |recipe| %>
        <li>
            <%= link_to recipe.name, recipe_path(recipe) %>
        </li>
    <% end %>
</ul>
